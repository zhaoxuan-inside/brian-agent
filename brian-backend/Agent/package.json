{
  "name": "@brian-agent/agent",
  "version": "1.0.0",
  "description": "Brian-Agent Agent 层模块集合",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@brian-agent/base": "file:../Base",
    "@brian-agent/core": "file:../Core"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  },
  "license": "Apache-2.0"
}
